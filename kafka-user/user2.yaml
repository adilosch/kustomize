apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser
metadata:
  name: user2
spec:
  authentication:
    type: scram-sha-512
  authorization:
    type: simple
    acls:
      - resource:
          type: topic
          name: example-topic-test
        operation: Read
